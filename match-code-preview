todos = []

while True:
    user_action = ("Type add, show, or exit:")

    match user_action:
        case 'add':
            todo = ("Enter a todo:")
            todos.append(todo)
        case 'show':
            print(todos)
        case 'exit':
            break
print("Bye!")
